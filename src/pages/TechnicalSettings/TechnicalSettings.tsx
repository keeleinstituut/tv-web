import { FC } from 'react'

import { useTranslation } from 'react-i18next'

// TODO: WIP - implement this page

const TechnicalSettings: FC = () => {
  const { t } = useTranslation()

  return <h1>TechnicalSettings</h1>
}

export default TechnicalSettings
