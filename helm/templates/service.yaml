apiVersion: v1
kind: Service
metadata:
  name: {{ include "tv-web.fullname" . }}
  labels:
    {{- include "tv-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tv-web.selectorLabels" . | nindent 4 }}
